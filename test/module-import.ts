import 'mocha';
import should = require('should');

import { IDiagramBrowserService } from '../src';
import { IHistoryService } from '../src';

describe('First test', () => {
    it('function', () => {

    });
});
