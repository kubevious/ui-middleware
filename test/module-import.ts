import 'mocha';
import should = require('should');

import { IDiagramService } from '../src';
import { IHistoryService } from '../src';

describe('First test', () => {
    it('function', () => {

    });
});
