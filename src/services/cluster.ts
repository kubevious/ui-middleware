import { IService } from '../common-types'

export interface IClusterService extends IService {

    // fetchClusterLatestSnapshot(clusterId: string);

    // fetchClusterSnapshots(clusterId: string);

    // fetchClusterConfig(clusterId: string);

    // createClusterConfig(clusterId: string, config: ClusterConfig);

    // deleteCluster(clusterId: string);

    // fetchClusterToken(clusterId: string);

    // resetClusterToken(clusterId: string);

    // fetchClusterReportStatus(clusterId: string);

    // subscribeClusterReportStatus(clusterId: string, cb: ((value: ClusterReportingStatus) => void));

}
